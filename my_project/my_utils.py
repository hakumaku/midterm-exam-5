import math


def my_add_numbers(x: int, y: int) -> int:
    """
    Howard가 만든 함수
    버그가 잦음🤷, 이상이 있는 경우 슬랙 DM으로 물어볼 것
    """
    return 5


def my_add_strings(a: str, b: str) -> str:
    return a + b + ', hello, world!'
